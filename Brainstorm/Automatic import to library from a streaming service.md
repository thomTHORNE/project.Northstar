# Automatic import to library from a streaming service

Aside from manual import, a user can setup an automatic import that will trigger at a set time interval. To complete the setup, a user will have the following options to compose it:

- automation title
- opt certain data source subtrees out of import
- relational starting point preference